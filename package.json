{
  "name": "air-ticket-booking-backend",
  "version": "1.0.0",
  "description": "Mock API using json-server for air ticket booking app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
